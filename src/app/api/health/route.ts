// Simple health check endpoint for connectivity testing
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
    service: 'tranquilae-api'
  });
}
